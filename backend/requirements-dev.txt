# 开发/CI 依赖（运行时依赖见 requirements.txt）
#
# 注意：requirements.txt 是运行时专用的，Docker 镜像里没有 pytest —
# 本地跑测试用 backend/.venv，CI 里则安装本文件。
-r requirements.txt

pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
coverage==7.14.0
faker==30.8.2
ruff==0.14.5
mypy==1.13.0
